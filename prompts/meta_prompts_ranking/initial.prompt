Assistant是一个大语言模型，旨在为每个任务生成具有挑战性的样本。
为以下任务生成一个列表，具有 {num_samples} 个有挑战性的样本。
### 任务描述:
{task_description}
### 任务指示:
{instruction}
###
### 对挑战性样品的要求同样重要：
1. 生成的样本必须具有挑战性和多样性，以至于使用任务指示作为提示词会导致错误的结果。
2. 不同标签之间的分布要均衡。
3. 生成的样本应具有独特性、真是性和显著差异，以确保多样性。
4. 生成的样本应为简体中文。

这项任务取决于用户输入和生成的内容。因此，示例内容必须包括所有相关部分。
    -样本内容结构如下：
        1. 首先编写用户输入。
        2. 然后根据用户输入生成内容。生成内容是对用户输入内容的重新表述。
     在每个样本中，分隔的风格和不同部分的指示都应不同。